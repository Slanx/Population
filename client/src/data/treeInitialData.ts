import { RawNodeDatum } from 'react-d3-tree/lib/types/types/common';

export const levels = [{ type: 'city' }, { type: 'district' }, { type: 'street' }];

export const root: RawNodeDatum = {
  name: 'Жители',
  children: [],
};
